# Claude Code Guidelines - 株式会社ゆだや vcv プロジェクト

## 厳守ルール
1. **ブランド表記**:
   - 「ファミリーマート」の名称・表記は一切使用禁止。
   - すべて「株式会社ゆだや」「ゆだや」「YUDAYA」としてブランディングすること。
2. **対象店舗**:
   - 2店舗構成（1号店＝旗艦店 / 2号店）。
   - 実店舗名・所在地は Firestore の `config/stores`（代表のみ読取可）に格納。
     コードおよび本ファイルには書かないこと（下記ルール5）。
3. **UI/UXの世界観**:
   - どうぶつの森風（Cozy Room）、温かみのある丸みフォント（M PLUS Rounded 1c）
   - アバター移動アニメーション（avatar-walk, avatar-work, pop-bubble）を維持すること。
4. **データ連携方針**:
   - 未認証ユーザー（一般来訪者）は「デモモード（モックデータ）」で動かすこと。
   - 湯田代表（認証UID）のみ Firebase (Cloud Firestore) 本番DBへ接続すること。

## セキュリティ厳守ルール（P0改修で追加）
5. **実店舗情報をコードに書かない**:
   - 実店舗名・住所・周辺情報（通り名、近隣ランドマーク等）・スタッフ実名を
     `index.html` や `src/`、および本ファイルに**絶対に書かないこと**。
     GitHub Pages で配信される＝全世界がソースを読める。
   - これらは Firestore の `config/stores`（代表のみ読取可）に置き、
     認証成功後に取得して表示する。
   - コードに置いてよいのはダミー表記（ゆだや1号店/2号店/ゆだやLab）のみ。
6. **代表判定をクライアントで行わない**:
   - UID比較やフラグ代入で「代表」と判定しないこと。
   - owner-only ドキュメントの読み取り成否で判定する。
     判定根拠は常に `firestore.rules` 側に置く。
